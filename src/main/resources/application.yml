logging:
  file: app.log
  path: /
  level:
    root: DEBUG

management:
    security:
        enabled : false


zuul:
    ignoredServices: '*'
    routes:
        auth-service-redirect:
            path: /api/auth/**
            sensitiveHeaders:
            url: http://knallani-1:8090/auth/
        auth-service:
            path: /api/**
            sensitiveHeaders:
            url: http://knallani-1:8090/auth/
    addHostHeader: true

ribbon:
  eureka:
    enabled: false