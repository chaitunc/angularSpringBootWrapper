logging:
  file: app.log
  path: /
  level:
    root: DEBUG

management:
    security:
        enabled : false


zuul:
    ignoredServices: '*'
    routes:
        auth-service:
            path: /api/**
            sensitiveHeaders:
            url: https://grim-coffin-41549.herokuapp.com/
    addHostHeader: true

ribbon:
  eureka:
    enabled: false